@@ set 
    domain          http://localhost:3000
    path            galen/specs/

@@table domain
	|domain|			|tags|			|url|
	|exeA|				|A|				|http://localhost:3000|
	|exeB|				|B|				|http://localhost:3000/?expuser=B/

@@ table devices
    |device|        |tags|          |size|
    |desktop|       |desktop|       |1920x1080|

@@ parameterized using devices
Home page on ${devices} device
    selenium chrome ${domain} ${size}
        check   ${path}header.gspec --include ${tags}